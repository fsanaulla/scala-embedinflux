version: 2
jobs:
  scalatest:
      docker:
        - image: circleci/openjdk:8-jdk
      steps:
        - checkout
        - run:
            name: Insatll sbt
            command: |
              wget -q https://dl.bintray.com/sbt/debian/sbt-1.1.0.deb
              sudo dpkg -i sbt-1.1.0.deb
        - run:
            name: Test
            command: "sbt \"project scalaTest\" +fullTest"
  specs2:
      docker:
        - image: circleci/openjdk:8-jdk
      steps:
        - checkout
        - run:
            name: Insatll sbt
            command: |
              wget -q https://dl.bintray.com/sbt/debian/sbt-1.1.0.deb
              sudo dpkg -i sbt-1.1.0.deb
        - run:
            name: Test
            command: "sbt \"project specs2\" +fullTest"
workflows:
  version: 2
  test:
    jobs:
      - scalatest
      - specs2