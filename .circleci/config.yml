version: 2
jobs:
  build:
       docker:
         - image: circleci/openjdk:8-jdk
       steps:
         - checkout
         - run:
             name: Insatll sbt
             command: |
                wget -q https://dl.bintray.com/sbt/debian/sbt-1.1.0.deb
                sudo dpkg -i sbt-1.1.0.deb
  test:
      docker:
        - image: circleci/openjdk:8-jdk
      steps:
        - run:
            name: Test
            command: "sbt \"+ clean\" \"+ compile\" \"+ test:compile\" \"+ test\""
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test:
          requires:
            - build